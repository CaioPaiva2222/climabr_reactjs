export const searchCity = (query) => {
  if (!query || query.trim().length < 3) {
    return [];
  }
};

export const searchCity2 = (query) => {
  if (!query || query.trim().length < 3) {
    return [];
  }
};
